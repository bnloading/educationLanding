import React from "react";
import CourseCatalog from "../CourseCatalog/CourseCatalog";

const Courses = () => {
  return (
    <div style={{ marginTop: 250, paddingInline: 50 }}>
      <CourseCatalog />
    </div>
  );
};

export default Courses;
